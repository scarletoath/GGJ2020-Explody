using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class StarLogic : MonoBehaviour
{
    public int roundPercentage;

    //Start the coroutine for the timer
    private void Start()
    {
        if (roundPercentage <= 0.2) //starts at 0.5 stars
        {
          
		}
        else if (roundPercentage <= 0.3)
        {
        
		}
        else if (roundPercentage <= 0.4)
        {
        
		}
        else if (roundPercentage <= 0.5)
        {
        
		}
        else if (roundPercentage <= 0.6)
        {
        
		}
        else if (roundPercentage <= 0.7)
        {
        
		}
        else if (roundPercentage <= 0.75)
        {
        
		}
        else if (roundPercentage <= 0.80)
        {
        
		}
        else if (roundPercentage <= 0.85)
        {
        
		}
        else if (roundPercentage <= 0.875)
        {
        
		}
        else if (roundPercentage <= 0.9) //Full three stars. Good job
        {
        
		}
    }
}
